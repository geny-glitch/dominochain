<% content_for :title, t("beta.actions_chaster.page_title", nickname: current_user.nickname) %>

<header class="ds-beta-page-head">
  <div class="ds-beta-page-head-row">
    <h1 class="ds-beta-page-title"><%= t("beta.catalog.actions.chaster.label") %></h1>
    <%= form_with url: beta_catalog_visibility_path, method: :patch, local: true, class: "ds-beta-catalog-toggle-form" do %>
      <%= hidden_field_tag :kind, "action" %>
      <%= hidden_field_tag :item_id, "chaster" %>
      <%= hidden_field_tag :enabled, "0" %>
      <%= hidden_field_tag :return_to, request.fullpath %>
      <label class="ds-form-label ds-beta-catalog-toggle" title="<%= t("beta.settings.toggle_aria", label: t("beta.catalog.actions.chaster.label")) %>">
        <%= check_box_tag :enabled, "1", beta_catalog_action_enabled?("chaster"), id: "catalog_action_chaster_page", class: "ds-beta-catalog-toggle-input", onchange: "this.form.requestSubmit();", "aria-label": t("beta.settings.toggle_aria", label: t("beta.catalog.actions.chaster.label")) %>
        <span class="ds-beta-catalog-toggle-switch" aria-hidden="true"></span>
      </label>
    <% end %>
  </div>
  <p class="ds-beta-page-lead">
    <%= t("beta.actions_chaster.lead") %>
  </p>
</header>

<%= render "actions_chaster" %>
