<% content_for :title, "Réglages — #{current_user.nickname}" %>

<header class="ds-beta-page-head">
  <h1 class="ds-beta-page-title">Réglages</h1>
  <p class="ds-beta-page-lead">
    Strava (pénalités), PiShock, vitrine publique et page backdoor.
  </p>
</header>

<%= render "settings_strava" %>
<%= render "settings_pishock" %>
<%= render "settings_showcase" %>
