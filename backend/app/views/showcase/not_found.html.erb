<% content_for :title, t("showcase.not_found.title") %>

<div class="ds-page">
  <div class="ds-container">
    <header class="ds-header">
      <%= image_tag "domino-chain-logo.png", alt: "Domino Chain", class: "ds-header-logo", width: 48, height: 48 %>
      <div>
        <h1 class="ds-title"><%= t("showcase.not_found.heading") %></h1>
        <p class="ds-subtitle"><%= t("showcase.not_found.subtitle") %></p>
      </div>
    </header>
    <p class="ds-text-muted"><%= link_to t("showcase.not_found.back_home"), root_path %></p>
  </div>
</div>
