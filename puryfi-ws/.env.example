BG_BACKEND_URL=https://bg-backend.fly.dev

# Debug : staticMediaScan détaillé + getPluginConfiguration (~1×/min + 1× au branchement)
# PURYFI_LOG_PLUGIN_PAYLOAD=1
# Fenêtre ms pour compter les hits du même fingerprint (logs seulement)
# PURYFI_SCAN_DEDUP_LOG_MS=120000